<Window x:Class="ManualMVVM.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManualMVVM.Views"
        xmlns:vm="clr-namespace:ManualMVVM.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="screenStatus" Text="{Binding ScreenStatus}"  Foreground="Black" FontSize="16" Margin="10" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding User.Id}" Foreground="Yellow" FontSize="16" HorizontalAlignment="Center" Margin="10"/>
            <TextBlock Text="{Binding User.Name}" Foreground="Green" FontSize="16" HorizontalAlignment="Center" Margin="10"/>
            <Button Content="Start Process" Command="{Binding screenRecordCmd}" Width="150" Height="40" Margin="10"/>
            <Button Content="Stop Screen" Command="{Binding stopScreenCmd}" Width="150" Height="40" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
