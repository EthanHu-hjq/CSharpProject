<Window x:Class="ModbusTcpClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModbusTcpClient"
        xmlns:vm="clr-namespace:ModbusTcpClient.ViewModels"
        d:DataContext="{d:DesignInstance vm:MainViewModel,IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="{x:Type ListViewItem}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="Warning">
                    <Setter Property="Background" Value="Yellow"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Error">
                    <Setter Property="Background" Value="LightCoral"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="IP Address:" Margin="5" VerticalAlignment="Center"/>
            <TextBox Text="{Binding IpAddress,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5"/>
            <TextBlock Text="Port:" Margin="5" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Port,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="5"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
            <StackPanel Orientation="Horizontal">
                <Rectangle Width="72"/>
                <Button Content="Get Front Door Closed" Command="{Binding GetFrontDoorClosedCommand}" IsEnabled="{Binding GetFrontDoorClosedEnable}"/>
                <Rectangle Width="5"/>
                <TextBox Text="{Binding TestReady,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"/>
                <Rectangle Width="31"/>
                <Button Content="Set Test Complete" Command="{Binding SetTestCompletedCommand}" IsEnabled="{Binding SetTestCompletedEnable}"/>
                <Rectangle Width="5"/>
                <TextBox Text="{Binding TestCompleted,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"/>
            </StackPanel>     
        </Border>

        <ListView Grid.Row="2" ItemsSource="{Binding LogEntries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Timestamp" DisplayMemberBinding="{Binding TimeStamp}" Width="150"/>
                    <GridViewColumn Header="Level" DisplayMemberBinding="{Binding Level}" Width="100"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}" Width="600"/>
                </GridView>
            </ListView.View> 
        </ListView>
    </Grid>
</Window>
