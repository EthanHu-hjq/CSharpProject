<DataGrid x:Class="Toucan_WPF.Ctrls.Ctrl_RecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Toucan_WPF.Ctrls" BorderThickness="0" HeadersVisibility="Column" CanUserReorderColumns="False"
             mc:Ignorable="d" GridLinesVisibility="All" IsReadOnly="True" AutoGenerateColumns="False"
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Export As csv..." Command="{Binding ExportResultAsList}"/>
        </ContextMenu>
    </DataGrid.ContextMenu>
    
    <DataGrid.Columns>
        <DataGridTextColumn Header="Idx" Binding="{Binding TempIndex}"/>
        <DataGridTextColumn Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
        <DataGridTemplateColumn Header="USL">
            <DataGridTemplateColumn.CellTemplateSelector>
                <local:ComparibleDataTemplateSelector >
                    <local:ComparibleDataTemplateSelector.TemplateString>
                        <DataTemplate>
                            <TextBlock Text="{Binding Limit.USL}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateString>
                    <local:ComparibleDataTemplateSelector.TemplateNumber>
                        <DataTemplate>
                            <Label Content="{Binding Limit.USL}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateNumber>
                    <local:ComparibleDataTemplateSelector.TemplateCurve>
                        <DataTemplate>
                            <Label>CURVE</Label>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateCurve>
                </local:ComparibleDataTemplateSelector>
            </DataGridTemplateColumn.CellTemplateSelector>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="LSL">
            <DataGridTemplateColumn.CellTemplateSelector>
                <local:ComparibleDataTemplateSelector>
                    <local:ComparibleDataTemplateSelector.TemplateString>
                        <DataTemplate>
                            <TextBlock Text="{Binding Limit.LSL,StringFormat={}{0:N3}}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateString>
                    <local:ComparibleDataTemplateSelector.TemplateNumber>
                        <DataTemplate>
                            <Label Content="{Binding Limit.LSL,StringFormat={}{0:F3}}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateNumber>
                    <local:ComparibleDataTemplateSelector.TemplateCurve>
                        <DataTemplate>
                            <Label>CURVE</Label>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateCurve>
                </local:ComparibleDataTemplateSelector>
            </DataGridTemplateColumn.CellTemplateSelector>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="Value">
            <DataGridTemplateColumn.CellTemplateSelector>
                <local:ComparibleDataTemplateSelector >
                    <local:ComparibleDataTemplateSelector.TemplateString>
                        <DataTemplate>
                            <TextBlock Text="{Binding Value,StringFormat={}{0:N3}}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateString>
                    <local:ComparibleDataTemplateSelector.TemplateNumber>
                        <DataTemplate>
                            <Label Content="{Binding Path=Value,StringFormat={}{0:F3}}"/>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateNumber>
                    <local:ComparibleDataTemplateSelector.TemplateCurve>
                        <DataTemplate>
                            <Label>CURVE</Label>
                        </DataTemplate>
                    </local:ComparibleDataTemplateSelector.TemplateCurve>
                </local:ComparibleDataTemplateSelector>
            </DataGridTemplateColumn.CellTemplateSelector>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="Unit" Binding="{Binding Limit.Unit}"></DataGridTextColumn>
        <DataGridTextColumn Header="Result" Binding="{Binding Result}"></DataGridTextColumn>
        <DataGridCheckBoxColumn Header="Skip" Binding="{Binding Skip}"></DataGridCheckBoxColumn>
        <DataGridTextColumn Header="Defect" Binding="{Binding Limit.Defect}"></DataGridTextColumn>
        <DataGridTextColumn Header="Time" Binding="{Binding TotalTime}"></DataGridTextColumn>

    </DataGrid.Columns>
    <DataGrid.RowStyle>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Result}" Value="0">
                    <Setter Property="Background" Value="Gray"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Result}" Value="3">
                    <Setter Property="Background" Value="Lime"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Result}" Value="4">
                    <Setter Property="Background" Value="Red"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Result}" Value="6">
                    <Setter Property="Background" Value="Yellow"></Setter>
                </DataTrigger>
                <DataTrigger Binding="{Binding Result}" Value="7">
                    <Setter Property="Background" Value="Purple"></Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </DataGrid.RowStyle>
</DataGrid>
