<Window x:Class="ApEngine.UIs.Setting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApEngine.UIs"
             mc:Ignorable="d" x:Name="self"
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl>
        <!--<TabItem Header="SignalPath Switch">
            <DockPanel>
                <DockPanel DockPanel.Dock="Top">
                    <TextBlock>if empty, the SignalPath Input Channel will not be updated</TextBlock>
                    <UniformGrid DockPanel.Dock="Top" Rows="1" HorizontalAlignment="Right">
                        <Label>SocketCount</Label>
                        <TextBox Margin="2" Width="40" HorizontalAlignment="Left"></TextBox>
                        <Button Margin="2" Click="btn_FromAp_Click">From AP</Button>
                        <Button Margin="2" Click="btn_Load_Click">Load</Button>
                        <Button Margin="2" Click="btn_SaveAs_Click">Save As</Button>
                    </UniformGrid>
                </DockPanel>
                <DataGrid Name="dg" CanUserAddRows="False" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding ElementName=self, Path=VariableItems}">
                </DataGrid>
            </DockPanel>
        </TabItem>-->
    </TabControl>
</Window>
