<Window x:Class="ScreenRecordTool.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScreenRecordTool.Views"
        xmlns:vm="clr-namespace:ScreenRecordTool.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Width="150" Text="{Binding TimerCount}"/>
            <CheckBox VerticalAlignment="Center" Content="is enable" IsChecked="{Binding IsEnable}"/>
            <Button Command="{Binding BtnClickCommand}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding BtnIcon}" FontFamily="Segoe MDL2 Assets"
                                 FontSize="16" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding BtnContent}" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel> 
    </Grid>
</Window>
